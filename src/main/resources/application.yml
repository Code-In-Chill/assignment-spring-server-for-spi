spring:
  application:
    name: remote-use-for-assignment-spi
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/remote_use_for_assignment_spi
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  main:
    allow-circular-references: true

server:
  port: 9799